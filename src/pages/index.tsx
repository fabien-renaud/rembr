import React from 'react';
import {NextPage} from 'next';
import {Layout} from 'components';

const Home: NextPage = () => {
    return (
        <Layout title="Home">
            <h1>Home page</h1>
        </Layout>
    );
};

export default Home;
