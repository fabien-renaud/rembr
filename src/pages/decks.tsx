import React from 'react';
import {Layout} from 'components';

const Decks = () => {
    return (
        <Layout title="Decks">
            <h1>Decks page</h1>
        </Layout>
    )
};

export default Decks;
