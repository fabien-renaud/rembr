import React from 'react';
import {Layout} from 'components';

const Decks: React.FC = () => {
    return (
        <Layout title="Decks">
            <h1>Decks page</h1>
        </Layout>
    );
};

export default Decks;
