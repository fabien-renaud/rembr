import React from 'react';
import {NavBar} from 'components';

export default function Decks() {
    return (
        <div>
            <h1>Decks page</h1>
            <NavBar />
        </div>
    )
};
