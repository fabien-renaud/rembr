import React from 'react';
import {Layout} from 'components';

const Profile = () => {
    return (
        <Layout title="Profile">
            <h1>Profile page</h1>
        </Layout>
    )
};

export default Profile;
